[project]
name = "mcp-database"
version = "0.1.0"
description = "FastMCP server for SQLite field service database operations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.1.1",
]
