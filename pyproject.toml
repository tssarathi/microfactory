[project]
name = "microfactory"
version = "0.1.0"
description = "AI Factory: Multi-agent field service operations platform"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.15.6",
]
