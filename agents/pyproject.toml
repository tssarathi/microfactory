[project]
name = "agents"
version = "0.1.0"
description = "Google ADK agents for field service, knowledge, and scheduling operations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.27.1",
    "litellm>=1.82.3",
    "mcp",
]
